import { useState } from "react";
import { de_CouncilX_backend } from "declarations/de-CouncilX-backend";

function App() {
   /* function handleSubmit(event) {
     event.preventDefault();
     const name = event.target.elements.name.value;
     de_CouncilX_backend.greet(name).then((greeting) => {
       setGreeting(greeting);
     });
     return false;
   } */

   return (
   <div>
   </div>
   );
}

export default App;
